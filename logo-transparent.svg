<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 48" fill="currentColor">
  <!-- Transparent SVG wordmark - Tech minimal, type-first, no background -->
  <!-- Uses currentColor to inherit text color from parent, ensuring visibility in both light & dark modes -->
  
  <!-- HEVIN wordmark -->
  <text x="0" y="32" font-size="36" font-weight="800" letter-spacing="-0.02em" font-family="Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">hevin</text>
  
  <!-- DESIGN label (smaller, below) -->
  <text x="0" y="46" font-size="10" font-weight="600" letter-spacing="0.1em" font-family="Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" opacity="0.6">DESIGN</text>
</svg>
